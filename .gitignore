# Python
.venv/
__pycache__/
*.pyc

# cocotb / simulation
test/sim_build/
sim_build/
build/
*.vcd
*.fst
*.fst.hier
results.xml
test/results.xml
test/gate_level_netlist.v

# Tiny Tapeout / hardening
runs/
tt_submission/
src/user_config.json
src/config_merged.json

# Vendored PDK liberty (fetch it once, see docs/ADAPTING.md)
pdk/*.lib

# Per-variant Yosys logs: 13 MB of transcript for numbers that are already
# committed in docs/synth/*.json and docs/synth/ppa.md. `make ppa` recreates
# them, and --reuse re-parses them if they are still around.
docs/synth/*.log
!docs/synth/top_shipped.full.log

# Editors
.DS_Store
.idea

# Formal proof work directories (scripts/run_formal.py regenerates them)
build/formal/
formal/*.sby
formal/*/
